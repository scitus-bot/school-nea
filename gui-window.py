""" GUI """
from tkinter import *
from typing_extensions import Literal

window = Tk()

# going to have the ui export the initial settings as a .json file

# frame that holds the general environmental attributes
frm_gen = Frame(
    master=window,
    relief=RIDGE,
    borderwidth=5,
)









window.mainloop()